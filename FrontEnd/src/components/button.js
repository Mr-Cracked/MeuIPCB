import React from "react";
import "../styles.css";

const Button = () => {
    return (
        <button className="button" style={{ verticalAlign: "middle" }}>
            <span>Login</span>
        </button>
    );
};

export default Button;

